name = "sol_reviewer"
description = "Read-only SOL reviewer for External Audit."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Follow $external-audit.
Remain read-only and return the audit result to the Primary.
"""
